#include <stdio.h>
int main(){
    int i,j,n;
    printf("Enter the number of rows ");
    scanf("%d", &n);
    for( i=0; i<=4; i++){
        for(j=0; j<i; j++)
        printf("%d", i);
        printf("\n");
    }
    return 0;
}
